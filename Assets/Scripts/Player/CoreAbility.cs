using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class CoreAbility
{
    // Start is called before the first frame update
    protected float consumeSpeed;
    
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
